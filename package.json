{
  "name": "dare-beauty-training-institute",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd frontend && next dev --port 3000",
    "dev:backend": "cd backend && tsx watch src/server.ts",
    "build": "cd frontend && next build",
    "start": "cd frontend && next start",
    "lint": "cd frontend && next lint",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
